name: (KS Youth) Frontend Lint, Unit and Component tests

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - ".github/workflows/ks-youth-frontend-tests.yml"
      - "frontend/kesaseteli/youth/**"
      - "frontend/kesaseteli/shared/**"
      - "frontend/shared/**"
      - "frontend/jest.config.js"
      - "frontend/tsconfig.json"
      - "frontend/next.config.js"
  workflow_dispatch:

jobs:
  common:
    uses: City-of-Helsinki/.github/.github/workflows/ci-node.yml@main
    secrets: inherit
    with:
      node-version: 22.x
      working-directory: ./frontend/kesaseteli/youth
      extra-commands: echo 'NEXTJS_DISABLE_SENTRY=true' >> .env
