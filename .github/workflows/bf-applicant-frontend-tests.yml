name: (BF-Applicant) Lint, Unit and Component tests

on:
  pull_request:
    paths:
      - ".github/workflows/bf-applicant-frontend-tests.yml"
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  common:
    uses: ./.github/workflows/ci-node2.yml
    secrets: inherit
    with:
      node-version: 22.x
      working-directory: ./frontend/benefit/applicant
      extra-commands: cd ./frontend/benefit/applicant && echo 'NEXTJS_DISABLE_SENTRY=true' >> .env
