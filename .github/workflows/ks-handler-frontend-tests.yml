name: (KS Handler) Frontend Lint, Unit and Component tests

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - ".github/workflows/ks-handler-frontend-tests.yml"
      - "frontend/kesaseteli/handler/**"
      - "frontend/kesaseteli/shared/**"
      - "frontend/shared/**"
      - "frontend/jest.config.js"
      - "frontend/tsconfig.json"
      - "frontend/next.config.js"
  workflow_dispatch:

jobs:
  common:
    uses: City-of-Helsinki/.github/.github/workflows/ci-node.yml@main
    secrets: inherit
    with:
      node-version: 22.x
      working-directory: ./frontend/kesaseteli/handler
      extra-commands: echo 'NEXTJS_DISABLE_SENTRY=true' >> .env
