{
  "name": "@frontend/emails",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "compile": "node src/emails/generate-emails.mjs",
    "clean": "rm -Rf src/emails/build/**",
    "export": "pnpm run clean && pnpm run compile && cp -R src/emails/build/ ../../../backend/benefit/helsinkibenefit/templates/emails/mjml-generated/"
  },
  "dependencies": {
    "mjml": "^4.18.0"
  }
}
