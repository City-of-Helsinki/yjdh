import styled from 'styled-components';

const StyledContainer = styled.div``;

const StyledHeading = styled.h1`
  font-size: 2rem;
  line-height: 2.4rem;
  margin: 0 0 1rem;
`;

export {
  StyledContainer,
  StyledHeading
}